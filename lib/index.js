/**
 * dsh-composer-resize — host half.
 * No host-side behavior; the real work is the browser bundle lib/client.js.
 */
export const name = 'dsh-composer-resize'
export const inject = []
export function apply() {}
